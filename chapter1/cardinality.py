def cardinality(array): 
#using set to get only unique values
#Utilizing print statement to make this easier for user to read.
	print("The cardinality of this set is :", str(len(set(array)))) 
numbers = [1, 2, 3, 4, 5, 5, 4, 3, 2, 1]
cardinality(numbers)